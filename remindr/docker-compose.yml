services:
  mongodb:
    # MongoDB 8 refuses to start on Docker Desktop's current Linux 6.19 kernel.
    image: mongo:7.0
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
volumes:
  mongodb_data:
